"""Alembic migration support for the app package.

The ``versions`` directory lives at the repo root (``alembic/versions``) per
``alembic.ini``. This module simply marks the package as importable.
"""
